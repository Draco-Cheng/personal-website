service:
  type: ClusterIP
  port: 8080
  targetPort: 3000

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "100m"

env:
  BACKEND_URL: "http://backend-service:8000"
